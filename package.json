{
  "name": "pi-network-airdrop-server",
  "version": "1.0.0",
  "description": "Server for handling Pi Network airdrop submissions and saving them to a JSON file.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"  // Optional: for local development with auto-reload
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"  // Add CORS support if needed
  },
  "devDependencies": {
    "nodemon": "^2.0.22"  // Optional: useful for development
  }
}
